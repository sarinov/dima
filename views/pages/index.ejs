<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head'); %>
</head>

<body>
    <%- include('../partials/header'); %>

    <% for(var i = posts.length - 1; i >= 0; i--) {%>
        <div class="post">
            <h2 class="post_title"><%= posts[i].title %></h2>
            <h2 class="post_title"><%= posts[i].description %></h2>
            <h2 class="post_title"><%= posts[i].img %></h2>
            <h2 class="post_title"><%= posts[i].viewed %></h2>
            <h2 class="post_title"><%= posts[i].content %></h2>
            <h2 class="post_title"><%= posts[i].userid %></h2>
        </div>
    <%} %>

    <%- include('../partials/footer'); %>
</body>
</html>
